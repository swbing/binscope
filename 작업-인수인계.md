# bin'scope 작업 인수인계 (2026-07-29 기준)

> **새 PC / 새 Claude 계정에서 이어서 작업할 때 이 문서부터 읽으세요.**
> 이전 대화의 기억은 넘어오지 않습니다. 여기 적힌 것이 전부입니다.
> 이 파일은 **공개 저장소에 있으므로 비밀번호·API 키를 절대 적지 마세요.**

---

## 0. 새 PC에서 시작하는 법 (제일 먼저)

```bash
git clone https://github.com/swbing/binscope.git
cd binscope
python collect.py        # 키 파일을 넣은 뒤에 실행
```

### ⚠️ 반드시 손으로 옮겨야 하는 파일 (git 에 없음, .gitignore 처리됨)

| 파일 | 내용 | 없으면 |
|---|---|---|
| `bizinfo_key.txt` | 기업마당 인증키(crtfcKey) 한 줄 | 수집 자체가 실패 |
| `kstartup_key.txt` | K-Startup serviceKey(64자리 hex) 한 줄 | K-Startup 176건이 통째로 빠짐 |

**이전 노트북의 이 두 파일을 USB·메일 등으로 새 PC 같은 폴더에 복사하세요.**
분실 시 재발급: 기업마당 <https://www.bizinfo.go.kr/apiDetail.do?id=bizinfoApi> 하단,
K-Startup 은 공공데이터포털(data.go.kr) 계정의 인증키.
`kstartup_key.txt` 의 키는 data.go.kr 공통키라 **다른 data.go.kr API 에도 그대로 통합니다**
(새 API 쓸 땐 키 재발급 없이 해당 API '활용신청'만 하면 됨 — 실측 확인함).

키 파일 대신 환경변수 `BIZINFO_KEY` / `KSTARTUP_KEY` 도 지원합니다.

### 필요 환경
- **Python 3** (표준 라이브러리만 사용, pip 설치 불필요)
- **git**
- Node.js 불필요

---

## 1. 이게 뭔가

전국 정부·공공 지원사업 공고를 매일 자동 수집해 보여주는 **정적 웹 대시보드**.
서버·DB 없음. GitHub Pages 호스팅 + GitHub Actions 크론.

- **라이브**: <https://swbing.github.io/binscope/>
- **저장소**: <https://github.com/swbing/binscope> (public)
- **로컬 폴더**: `C:\Users\소우빈\OneDrive\Desktop\우빈 정부지원사업\` = 이 repo
- **브랜드 표기**: `bin'scope` (작은따옴표 포함). repo·URL 은 따옴표를 못 쓰므로 `binscope`.

### 현재 규모 (2026-07-29)
**1,721건** = 기업마당 1,534 + K-Startup 176 + 커넥트웍스 11
첨부 공고문 링크 3,819개 / `grants-data.js` 3.9MB (gzip 전송 약 580KB)

---

## 2. 파일 구조

| 파일 | 역할 |
|---|---|
| `index.html` | **앱 본체. 편집은 이 파일.** 단일 HTML(HTML+CSS+JS 전부) |
| `널핏_지원사업 서칭.html` | 옛 주소 호환용 리다이렉트. **지우지 마세요** |
| `collect.py` | 수집기. Python 표준 라이브러리만 |
| `grants-data.js` | 수집 결과(`window.GRANTS`). 앱이 `<script src>` 로 읽음 |
| `grant_summaries.json` | 사람이 만든 AI 요약 캐시 (pblancId 키, 80건뿐) |
| `g/*.html` | 공고별 정적 페이지 1,721개 (검색 노출용, 자동 생성) |
| `g/index.html` | 전체 공고 목록 (크롤러 내부링크용, 자동 생성) |
| `sitemap.xml` | 자동 생성 (1,723 URL) |
| `googlef0353fa67a428eca.html` | **구글 소유권 확인 파일. 지우면 인증 풀림** |
| `.github/workflows/update.yml` | 매일 06:00 KST 자동 수집 |

---

## 3. 배포 방법 (중요 — 순서를 지키세요)

Actions 봇이 매일 `grants-data.js` 등을 main 에 커밋하므로 **pull 을 먼저** 해야 합니다.

```bash
git pull origin main --no-rebase -X theirs --no-edit
git add -A
git commit -m "메시지"
git push origin main
```

pull 을 빼먹으면 non-fast-forward 로 거부됩니다. 반영은 push 후 약 1분.

### GitHub Secrets (등록 완료 상태)
`BIZINFO_KEY`, `KSTARTUP_KEY` 둘 다 등록됨.
**KSTARTUP_KEY 가 빠지면 매일 갱신 때 K-Startup 176건이 조용히 사라집니다.**

---

## 4. 데이터 수집 구조

```
collect.py
 ├─ 기업마당 오픈API   bizinfo.go.kr/uss/rss/bizinfoApi.do   (crtfcKey)
 ├─ K-Startup 오픈API  nidapi.k-startup.go.kr/...            (serviceKey)
 ├─ 커넥트웍스         works.connect24.kr                    (키 불필요)
 ├─ attach_files()     상세페이지에서 첨부 다운로드 URL 추출 (ThreadPool 8)
 └─ write_pages()      g/*.html + g/index.html + sitemap.xml
```

### 소스별 핵심 사실 (다시 조사하지 마세요)

**기업마당** — API 실측 상한 **1,534건**. `searchCnt` 를 2000·3000 으로 올려도 동일.
`MAX_ITEMS=2000` 으로 두면 전량 수집. 접수중만 반환됨.
등록일은 **`creatPnttm`** 사용 (`updtPnttm` 은 전체 일괄갱신이라 쓰면 안 됨).

**K-Startup** — 엔드포인트가 `apis.data.go.kr` 가 아니라 **`nidapi.k-startup.go.kr`**.
(apis.data.go.kr 의 구버전 경로는 401. `kisedKstartupService01/...01` 경로는 200 확인)
**등록일 필드가 없어** 접수시작일(`pbanc_rcpt_bgng_dt`)을 대용으로 씀.
딥링크는 `pbanc_sn` 사용 (`id` 는 단순 순번이라 쓰면 안 됨).

**커넥트웍스** — 카드가 `data-*` 속성에 값을 통째로 담고 있어 본문 파싱 불필요.
`?page=N` 서버 페이지네이션 작동. 접수중은 1~30페이지에 집중(50페이지부터 0).
⚠️ **이 사이트는 지난 공고를 전부 보관하므로 '마감일 없음'을 상시로 처리하면 안 됨**
(깊은 페이지의 '접수중'은 전부 마감일 빈 옛 공고) → 미래 마감일 확인된 것만 수용.

---

## 5. ⛔ 이미 조사해서 막힌 것 (재시도 금지 — 시간 낭비)

**theVC (thevc.kr)** — 파서 불가 확정.
403 은 봇차단이 아니라 UA 문제였고 브라우저 UA 면 200. 진짜 막힘은:
① `/grants` 목록이 **SSR 10건 고정**, `?page`·`?limit`·`?sort` 를 서버가 전부 무시
② 나머지 2,500여 건은 `/api` 로만 오는데 **robots.txt 가 `Disallow: /api` 로 명시 차단**
③ sitemap 은 17.5만 건 전체 이력에 `lastmod` 조차 없어 접수중만 거를 수단이 없음

**NTIS (ntis.go.kr)** — `robots.txt` 가 **`Disallow: /`** 로 사이트 전체 크롤링 금지.
(단 공식 Open API 는 별도로 존재하므로 그 경로는 가능)

**SBA(sba.seoul.kr) / startup-plus.kr** — 목록이 JS 렌더라 HTML 파싱 불가.

> **원칙: robots.txt 로 막힌 곳은 공개 배포물이라 쓰지 않습니다. 공식 Open API 경로를 찾으세요.**

### 💡 가장 중요한 교훈
커넥트웍스는 접수중 609건 중 **신규가 11건(1.8%)뿐**이었습니다.
457건은 기업마당에, 141건은 K-Startup 에 이미 있었습니다.
기업마당을 500→1,534 로 늘리는 순간 지역 테크노파크·진흥원 공고를 대부분 흡수했기 때문입니다.
**→ 새 애그리게이터를 붙이는 것보다 기존 1차 소스의 수집 상한을 푸는 게 훨씬 이득이 큽니다.**

---

## 6. 코드에서 건드리면 안 되는 것

- **`localStorage` 키 `binscope_seen_guide`** — 바꾸면 기존 사용자 전원에게 온보딩 투어가 다시 뜸
- **`localStorage` 키 `nurfit_dashboard_v6`** — 사용자 설정 저장소
- **`googlef0353fa67a428eca.html`** 과 `index.html <head>` 의 `google-site-verification` meta
  → 둘 중 하나만 지워도 구글 소유권이 풀림
- **공고 페이지 주소(`sid`)** — 반드시 원본 고유 ID 기반
  (`biz-<pblancId>` / `ks-<pbancSn>` / `cw-<data-id>`).
  기존 `id`(10000+순번)는 **매일 값이 바뀌므로** URL 로 쓰면 SEO 가 전부 무효화됨

### 설정이 기기마다 다른 이유 (버그 아님)
설정은 `localStorage` 에 저장되고 이건 **주소(origin)별로 분리**됩니다.
`file://` · `http://localhost` · `https://swbing.github.io` 는 서로 다른 저장소입니다.
PC↔휴대폰 동기화도 안 됩니다. 공개 사이트에선 이게 맞는 동작(방문자별 개인 설정).

### 알려진 문제
- **관리자 비밀번호가 공개 저장소의 `index.html` 에 평문으로 들어 있습니다**
  (`store.adminPw` 기본값). 작업자 뷰 보호용이라 위험도는 낮지만,
  **공개돼 있다는 사실을 알고 계셔야 하고 바꾸려면 코드에서 고쳐야 합니다.**
- 요약 캐시가 **80건뿐** → 나머지 1,641건은 기본 템플릿 요약이라 상세 품질이 낮음

---

## 7. 검색 노출(SEO) 현황

| 항목 | 상태 |
|---|---|
| 구글 소유권 확인 | ✅ 완료 (HTML 태그 방식) |
| 구글 색인 요청 | ✅ 요청함 (2026-07-29) |
| sitemap 제출 | ✅ 제출함. 상태 "가져올 수 없음" = 아직 미수집(정상, 2~3일 대기) |
| 메타데이터 | ✅ description·canonical·OG·JSON-LD·noscript |
| 공고별 페이지 | ✅ 1,721개 |
| 외부 링크 | ✅ README 경유 (GitHub 저장소 페이지) |
| GitHub About Website 칸 | ⬜ 미설정 (3클릭, 계정 권한 필요) |
| 네이버 | ⬜ 보류. **호스트 단위 URL 만 받아서 `/binscope/` 하위경로는 거부됨** |

**robots.txt 는 일부러 안 만들었습니다.** GitHub Pages 프로젝트 사이트는
`swbing.github.io/robots.txt`(루트)만 읽고 `/binscope/robots.txt` 는 무시합니다.
루트가 404 라 크롤링은 이미 전면 허용 상태입니다.

### 검색에 나오기까지
색인은 **며칠~몇 주** 걸립니다. 요청은 '줄 서기'일 뿐 즉시 반영이 아닙니다.
`site:swbing.github.io/binscope` 로 검색하면 진행 상황을 볼 수 있습니다.

**"정부지원사업" 같은 대형 키워드 상위 노출은 현실적으로 어렵습니다.**
기업마당·K-Startup·정부24 가 상위를 잡고 있고 `github.io` 하위경로는 도메인 신뢰도가 약합니다.
현실적 목표는 ① 사이트명 검색 노출 ② **개별 공고명 롱테일 유입**(공고별 페이지가 그래서 있음).
대형 키워드를 노리려면 **자체 도메인 구입 + 외부 링크 확보 + 콘텐츠 축적**이 필요하며,
그래도 수개월 단위 작업입니다.

**네이버를 하려면**: `swbing.github.io` 라는 이름의 저장소를 새로 만들어야 합니다
(GitHub 규칙상 그 이름이 루트를 담당). 그러면 루트 소유확인·robots.txt·루트 404 해소가 한번에 됩니다.
다만 네이버는 외부 정적 사이트 색인이 인색해 투자 대비 효과가 불확실합니다.

---

## 8. 다음에 하면 좋은 것 (우선순위 순)

1. **요약 품질** — 1,641건이 기본 템플릿 요약. Claude 가 세션에서 직접 요약해
   `grant_summaries.json` 에 넣거나(현재 방식), Anthropic API 키를 쓰면 자동화됨
   (`collect.py` 에 호출부는 이미 구현돼 있고 키만 없으면 캐시로 폴백)
2. **설정 내보내기/가져오기** — PC↔휴대폰 설정 이동용 JSON
3. **GitHub About Website 칸 설정** (3클릭)
4. **워크플로 안전장치** — 수집 결과가 이전보다 크게 줄면 커밋 거부
   (키 누락 시 데이터가 조용히 반토막 나는 것 방지)
5. 자체 도메인 → robots.txt·네이버·SEO 신뢰도 한번에 해결

---

## 9. 새 Claude 세션에 처음 할 말 (복붙용)

```
binscope 정부지원사업 대시보드 작업을 이어서 하려고 해.
폴더의 '작업-인수인계.md' 를 먼저 읽고, collect.py 와 index.html 구조를 파악한 뒤
현재 상태를 요약해줘. 그 다음 할 일은 그때 정하자.
```
